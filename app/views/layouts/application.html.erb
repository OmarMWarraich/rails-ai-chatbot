<!DOCTYPE html>
<html>
  <head>
    <title>OpenAi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <script src="https://cdn.tailwindcss.com"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="flex min-h-screen">
      <% if current_user.present? %>
        <div class="flex w-52 bg-gray-900 text-white">
          <div class="container mx-auto py-5">
            <div class="flex items-center justify-between border px-12 border-red-500">
              <%= form_with(model: Chat.new) do |form| %>
                <%= form.submit '+ New Chat', class: 'text-white mt-2 bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800' %>
              <% end %>
            </div>
            <div class="mt-3">
              <h4>Today:</h4>
              <turbo-frame id="user-chats">
                <%= render partial: 'chats/chat_list', collection: current_user.chats.reverse if current_user.present? %>
              </turbo-frame>
            </div>
          </div>
        </div>
      <% end %>
      <div class="container mx-auto py-5">
        <div class="flex justify-center">
          <div class='w-10/12'>
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </html>
